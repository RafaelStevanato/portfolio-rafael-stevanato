import { en } from './en'
import { ptBR } from './pt-BR'

// Objeto que mapeia o idioma selecionado para suas traduções
// Uso: const t = translations[language] → t.nav.about, t.nav.projects...
export const translations = {
  'en': en,
  'pt-BR': ptBR,
}